import { ThanosSnapDirective } from './thanos-snap.directive';

describe('ThanosSnapDirective', () => {
  it('should create an instance', () => {
    const directive = new ThanosSnapDirective();
    expect(directive).toBeTruthy();
  });
});
